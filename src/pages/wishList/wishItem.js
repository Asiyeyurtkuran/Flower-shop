import React, { useContext } from 'react'
import { ShopContext } from '../../context/context';

const WishItem = () => {
    // const { id, productName, category, subCategory, description, productImage, price } = props.data;
    // const { wishItems, removeFromWishList } = useContext(ShopContext);


    // return (
    //     <div className='wishItem'>
    //         <img src={productImage} alt='' />
    //         <div className='wishItemInfo'>
    //             <h3 className='wishItemTitle'> {productName} </h3>
    //             <p className='wishItemPrice'> £ {price} </p>
    //         </div>
    //         <button className='removeBttn' onClick={() => removeFromWishList(id)}>
    //             Remove</button>
    //     </div>

    // )
}

export default WishItem

